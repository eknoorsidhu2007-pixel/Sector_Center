NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
FINNHUB_API_KEY=
TWELVE_DATA_API_KEY=
ALPHA_VANTAGE_API_KEY=
# Required for SEC company-identity seed (`npm run research:seed-companies`).
# EDGAR needs no API key, but every request must send a User-Agent that
# identifies you with contact info, e.g. "Sector Center you@example.com".
# Do not invent a placeholder. Server-side only; never NEXT_PUBLIC_*.
SEC_USER_AGENT=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
